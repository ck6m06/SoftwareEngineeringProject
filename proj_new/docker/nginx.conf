# ========================================
# Nginx 配置 - GCP 生產環境版本
# ========================================
# 用途: 用於 GCP VM 部署，包含安全性和效能優化
# 使用: 複製到 docker/nginx.conf 或在 docker-compose-gcp.yml 中掛載
# ========================================

server {
    listen 80;
    # 接受所有域名和 IP (GCP 部署時可以指定具體域名)
    server_name _;
    
    root /usr/share/nginx/html;
    index index.html;

    # 安全性標頭
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Gzip 壓縮
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private must-revalidate auth;
    gzip_types text/plain text/css text/xml text/javascript 
               application/x-javascript application/xml+rss application/json;
    gzip_disable "MSIE [1-6]\.";

    # 限制請求大小 (防止大檔案上傳攻擊)
    client_max_body_size 50M;

    # 超時設定
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # SPA 路由處理
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        # GCP 環境使用容器名稱
        proxy_pass http://backend:5000/api/;
        proxy_http_version 1.1;
        
        # WebSocket 支援
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        
        # 保留原始資訊
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # 傳遞真實 IP (重要！用於日誌和安全)
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS 預檢請求快速回應
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $http_origin;
            add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, PATCH, OPTIONS';
            add_header Access-Control-Allow-Headers 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type 'text/plain; charset=utf-8';
            add_header Content-Length 0;
            return 204;
        }
    }

    # MinIO 物件儲存代理 (圖片訪問)
    location /storage/ {
        proxy_pass http://minio:9000/pet-adoption/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 圖片快取 1 週
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 靜態資源快取
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 禁止訪問隱藏檔案
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 健康檢查端點
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# ========================================
# HTTPS 配置 (使用 Let's Encrypt)
# ========================================
# 取消註解以啟用 HTTPS
# server {
#     listen 443 ssl http2;
#     server_name pet-adoption.yourdomain.com;
#     
#     ssl_certificate /etc/letsencrypt/live/pet-adoption.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pet-adoption.yourdomain.com/privkey.pem;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # 其他設定與上方相同
#     root /usr/share/nginx/html;
#     index index.html;
#     
#     # ... (複製上方所有 location 區塊)
# }
# 
# # HTTP 自動重定向到 HTTPS
# server {
#     listen 80;
#     server_name pet-adoption.yourdomain.com;
#     return 301 https://$server_name$request_uri;
# }
