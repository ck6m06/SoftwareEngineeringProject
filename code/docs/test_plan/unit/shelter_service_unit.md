# Shelter Service 單元測試計劃

## 文件說明
本文件記錄 ShelterService 的所有單元測試案例，共計 45 個測試案例。

**備註**：原始測試文件中有兩個同名的 `TestUpdateShelterEdgeCases` 類別，Python 只保留最後一個定義，因此前面類別中的 2 個測試方法（`test_update_address_missing_fields` 和 `test_update_address_invalid_type`）實際上不會被 pytest 執行。

---

**測試編號：UT-SHELTERSERVICE-001**

**測試目標**：測試管理員有完全權限  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-002**

**測試目標**：測試收容所成員對自己收容所有權限  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期

**測試角色**：收容所成員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-003**

**測試目標**：測試收容所成員對其他收容所沒有權限  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：收容所成員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-004**

**測試目標**：測試用戶不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.check_shelter_permission()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-005**

**測試目標**：測試普通用戶沒有權限  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：收容所成員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-006**

**測試目標**：測試成功創建收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.create_shelter()` 方法
3. 驗證結果符合預期

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-007**

**測試目標**：測試無效的 Email 格式  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.create_shelter()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-008**

**測試目標**：測試地址缺少必填欄位  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.create_shelter()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-009**

**測試目標**：測試 Slug 已存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.create_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-010**

**測試目標**：測試地址不是字典類型  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.create_shelter()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-011**

**測試目標**：測試成功更新收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.update_shelter()` 方法
3. 驗證結果符合預期

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-012**

**測試目標**：測試收容所不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.update_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-013**

**測試目標**：測試更新無效的 Email  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.update_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-014**

**測試目標**：測試更新重複的 Slug  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.update_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-015**

**測試目標**：測試管理員成功驗證收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.verify_shelter()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-016**

**測試目標**：測試非管理員無法驗證  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.verify_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-017**

**測試目標**：測試收容所不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.verify_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-018**

**測試目標**：測試成功驗證 CSV  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試數據
- 執行被測試的服務方法
- 驗證返回結果或異常

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-019**

**測試目標**：測試未選擇檔案  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-020**

**測試目標**：測試錯誤的檔案格式  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-021**

**測試目標**：測試檔案過大  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-022**

**測試目標**：測試空的 CSV 檔案  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-023**

**測試目標**：測試無效的編碼  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 呼叫 `ShelterService.validate_csv_file()` 方法
2. 驗證結果符合預期
3. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-024**

**測試目標**：測試成功列出收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.list_shelters()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-025**

**測試目標**：測試搜尋收容所  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.list_shelters()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-026**

**測試目標**：測試只列出已驗證的收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.list_shelters()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-027**

**測試目標**：測試成功獲取收容所  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.get_shelter()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-028**

**測試目標**：測試收容所不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.get_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-029**

**測試目標**：測試成功批次更新動物狀態  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-030**

**測試目標**：測試收容所不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-031**

**測試目標**：測試無效的操作類型  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-032**

**測試目標**：測試動物不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-033**

**測試目標**：測試成功上傳檔案  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.upload_file_to_minio()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-034**

**測試目標**：測試不支援的檔案類型  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.upload_file_to_minio()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-035**

**測試目標**：測試檔案過大  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.upload_file_to_minio()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-036**

**測試目標**：測試 MinIO 上傳失敗  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.upload_file_to_minio()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-037**

**測試目標**：測試成功創建批次匯入任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.create_batch_import_job()` 方法
3. 驗證結果符合預期

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-038**

**測試目標**：測試收容所不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.create_batch_import_job()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-039**

**測試目標**：測試批次轉為草稿  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-040**

**測試目標**：測試批次提交審核  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-041**

**測試目標**：測試批次下架  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-042**

**測試目標**：測試無效的狀態轉換  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.batch_update_animal_status()` 方法
3. 驗證結果符合預期

**測試角色**：系統  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-043**

**測試目標**：測試收容所成員可以創建新收容所（無 shelter_id）  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期

**測試角色**：收容所成員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-044**

**測試目標**：測試收容所主要負責人有權限  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.check_shelter_permission()` 方法
3. 驗證結果符合預期

**測試角色**：收容所成員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-SHELTERSERVICE-045**

**測試目標**：測試更新時地址缺少必填欄位  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `ShelterService.update_shelter()` 方法
3. 驗證結果符合預期
4. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

