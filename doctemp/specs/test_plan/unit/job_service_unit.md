# Job Service 單元測試計劃

## 文件說明
本文件記錄 JobService 的所有單元測試案例，共計 18 個測試案例。

---

**測試編號：UT-JOBSERVICE-001**

**測試目標**：TC-01: 管理員可以查看所有任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.list_jobs()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.list_jobs()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-002**

**測試目標**：TC-02: 普通用戶只能看到自己的任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.list_jobs()` 方法
3. 驗證結果符合預期

**測試角色**：一般會員  
**測試流程**：
- 執行 JobService.list_jobs()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-003**

**測試目標**：TC-03: 按類型過濾任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.list_jobs()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.list_jobs()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-004**

**測試目標**：TC-04: 無效的狀態過濾  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.list_jobs()` 方法
3. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-005**

**測試目標**：TC-04a: 管理員可以按創建者過濾  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.list_jobs()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.list_jobs()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-006**

**測試目標**：TC-05: 管理員可以查看任何任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.get_job()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.get_job()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-007**

**測試目標**：TC-06: 用戶可以查看自己的任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.get_job()` 方法
3. 驗證結果符合預期

**測試角色**：一般會員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-008**

**測試目標**：TC-07: 用戶不能查看他人的任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.get_job()` 方法
3. 確認拋出預期的異常

**測試角色**：一般會員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-009**

**測試目標**：TC-08: 任務不存在  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.get_job()` 方法
3. 確認拋出預期的異常

**測試角色**：一般用戶  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-010**

**測試目標**：TC-09: 成功重試失敗的任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.retry_job()` 方法
3. 驗證結果符合預期

**測試角色**：一般會員  
**測試流程**：
- 執行 JobService.retry_job()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-011**

**測試目標**：TC-10: 不能重試非失敗的任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.retry_job()` 方法
3. 確認拋出預期的異常

**測試角色**：一般會員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-012**

**測試目標**：TC-11: 成功取消待處理的任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.cancel_job()` 方法
3. 驗證結果符合預期

**測試角色**：一般會員  
**測試流程**：
- 執行 JobService.cancel_job()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-013**

**測試目標**：TC-12: 不能取消已完成的任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.cancel_job()` 方法
3. 確認拋出預期的異常

**測試角色**：一般會員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-014**

**測試目標**：TC-13: 管理員成功核准任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.approve_job()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.approve_job()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-015**

**測試目標**：TC-14: 非管理員不能核准任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.approve_job()` 方法
3. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-016**

**測試目標**：TC-15: 不能核准非待處理的任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.approve_job()` 方法
3. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-017**

**測試目標**：TC-16: 管理員成功拒絕任務  
**測試方法**：等價劃分 (Equivalence Partitioning)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.reject_job()` 方法
3. 驗證結果符合預期

**測試角色**：平台管理員  
**測試流程**：
- 執行 JobService.reject_job()
- 驗證結果值正確

**測試結果**：✅ 通過

---

**測試編號：UT-JOBSERVICE-018**

**測試目標**：TC-17: 非管理員不能拒絕任務  
**測試方法**：錯誤推測 (Error Guessing)  
**測試步驟**：
1. 準備測試環境（Mock 必要的依賴對象）
2. 呼叫 `JobService.reject_job()` 方法
3. 確認拋出預期的異常

**測試角色**：平台管理員  
**測試流程**：
- 準備測試環境和測試數據
- 執行被測試的服務方法
- 驗證返回結果符合預期

**測試結果**：✅ 通過

---

